server:
  port: 8082

spring:
  application:
    name: soa-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.reactive.ReactiveSecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.reactive.ReactiveUserDetailsServiceAutoConfiguration

jwt:
  secret: ${JWT_SECRET:?JWT_SECRET must be set}

services:
  auth:        ${AUTH_SERVICE_URL:http://localhost:8080}
  profile:     ${PROFILE_SERVICE_URL:http://localhost:8081}
  filemanager: ${FILEMANAGER_SERVICE_URL:http://localhost:8083}

logging:
  level:
    org.springframework.cloud.gateway: INFO
    mn.icsi437.gateway: INFO